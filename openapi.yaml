openapi: 3.1.0
info:
  title: raphael-workflows
  version: 0.1.0
  description: Approval/review/publish workflow engine
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
